= render 'layouts/header'
= render 'layouts/left_bar'

#mainBar
	.friendRequestHeader
		%h2 Friend Requests

	.friendRequestList
		- if @friend_requests.length == 0
			%p No new requests
		- else
			= render @friend_requests

