= form_for @message_receiver do |f|
  -if @message_receiver.errors.any?
    #error_explanation
      %h2= "#{pluralize(@message_receiver.errors.count, "error")} prohibited this message_receiver from being saved:"
      %ul
        - @message_receiver.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :message_id
    = f.number_field :message_id
  .field
    = f.label :user_id
    = f.number_field :user_id
  .actions
    = f.submit 'Save'
