= form_for @group_moderator do |f|
  -if @group_moderator.errors.any?
    #error_explanation
      %h2= "#{pluralize(@group_moderator.errors.count, "error")} prohibited this group_moderator from being saved:"
      %ul
        - @group_moderator.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :group_id
    = f.number_field :group_id
  .field
    = f.label :user_id
    = f.number_field :user_id
  .actions
    = f.submit 'Save'
